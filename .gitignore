# Repo-agnostic base — will be superseded by the devopser skeleton.

# OS / editor
.DS_Store
*.swp

# pnpm content-addressable store (lands at repo root in this container; a cache,
# never committed)
.pnpm-store/

# node
node_modules/

# --- Go block ---
# Binaries
/chater
/bin/
*.exe
*.test
*.out

# Coverage
coverage.out
coverage.txt

# Local env / dev database (SQLite lands in a later step)
*.local
*.env
*.db
*.sqlite
*.sqlite3

# Go workspace file (never commit)
go.work
go.work.sum

# >>> omnifield-skeleton (managed by devopser; синк: init.mjs, не редактировать руками) >>>
# Node
node_modules/
dist/
.vite/
*.local
# *.local — vite env; extension-point'ы эталонных хуков НЕ глотать (feedback brainer П1)
!.husky/*.local
.nx/
coverage/
*.tsbuildinfo
# pnpm store падает в workspace, когда home и проект на разных devices (devbox 2а, Д4)
.pnpm-store/

# Python (uv)
.venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# env
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
out-tsc/

# Claude Code runtime (harness state — hooks/settings трекаются, runtime нет)
.claude/.main-session-id
.claude/settings.local.json
# <<< omnifield-skeleton <<<
